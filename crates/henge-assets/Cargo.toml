[package]
name = "henge-assets"
description = "Logical asset ids resolved through a stack of packs, so artwork can be replaced one piece at a time."
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
anyhow = "1"
png = "0.17"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
